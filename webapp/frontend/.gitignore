# TypeScript incremental build cache (generated by `tsc -b`, machine-specific,
# not meant to be committed). node_modules/ and dist/ for this directory are
# already covered by the repo root .gitignore.
*.tsbuildinfo
